# UAL-CCI-ML-1
# All markdown documentation and code are written and executed using Jupyter Notebook (.ipynb), which allows explanations, outputs, and visualisations to be shown together in one place.

## Environment / libraries

### Task 1
- pandas
- numpy
- scikit-learn
- matplotlib
- joblib

### Task 2
- pandas
- numpy
- joblib
- (uses standard library: collections, os: no extra install)

### Task 3
- numpy
- keras (usually installs TensorFlow as backend)
- matplotlib
- joblib
- (uses standard library: os, pickle, pathlib, shutil: no extra install)

### If you're using one env for everything
Install: pandas, numpy, scikit-learn, matplotlib, joblib, keras (or tensorflow). That should cover all three tasks.
